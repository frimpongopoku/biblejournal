# dependencies
node_modules/
.pnp
.pnp.js

# env files
.env
.env.local
.env.production

# next.js build
.next/
out/

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# Embedding checkpoint (temporary, only exists mid-run)
data/embed-progress.json

# Bible embeddings binary — un-ignored, committed for Vercel deployment
# If bundle size becomes an issue, migrate to Vercel Blob Storage
# data/bible-embeddings.bin
